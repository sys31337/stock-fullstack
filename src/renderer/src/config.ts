const config = {
  backoffice: 'https://discrete-needlessly-pangolin.ngrok-free.app',
  socket: 'http://localhost:4031',
};

export default config;

/*
the back office link must link to external link that has the backoffice embeded as iFrame;
socket: an internal URL for socket initialized in ~/.src/main/api/main.ts
*/
